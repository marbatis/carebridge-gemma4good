# Optional local dependencies for the CareBridge Gemma backend.
# Kaggle runtimes usually already include torch, so local installs are the main use case here.

accelerate>=1.0,<2
huggingface-hub>=0.34,<1.0
sentencepiece>=0.2,<1
transformers>=4.50,<5
